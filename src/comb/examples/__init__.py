"""Reusable example ``System`` constructions.

Each module exposes a class whose ``__init__`` builds a ready-to-solve system;
instances carry named attributes for the bodies and constraints (e.g.
``ex.base``, ``ex.joint``) plus ``ex.system``. Useful for tests, demos, and
downstream rendering.
"""
