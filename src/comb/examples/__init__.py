"""Reusable example ``Mode`` constructions.

Each module exposes a class whose ``__init__`` builds a ready-to-solve mode;
instances carry named attributes for the bodies and constraints (e.g.
``ex.base``, ``ex.joint``) plus ``ex.mode``. Useful for tests, demos, and
downstream rendering.
"""
